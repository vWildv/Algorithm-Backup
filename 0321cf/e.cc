#include <cstdio>
#include <cstring>
#include <iostream>
#include <algorithm>
using namespace std;

struct node{
    int l,r,c;
}a[200005];

int main(){
    int n,x;
    scanf("%d%d",&n,&x);
    for(int i=0;i<n;i++) scanf("%d%d%d",&a[i].l,&a[i].r,&a[i].c);
    
}